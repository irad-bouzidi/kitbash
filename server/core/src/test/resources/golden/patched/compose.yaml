services:
  api:
    build: "."
    ports:
    - 8080:8080
    environment:
      JWT_ISSUER: "${JWT_ISSUER:-http://keycloak:8080/realms/kitbash}"
  db:
    image: postgres:16-alpine
  web:
    build: ./web
    ports:
    - 5173:5173
    depends_on:
    - api

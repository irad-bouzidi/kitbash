import { registerRootComponent } from 'expo';

import App from './App';

// Expo's entry point. `registerRootComponent` is `AppRegistry.registerComponent` plus the
// environment setup Expo Go and a development build both need, so this file is the one place a
// generated app differs from a bare React Native one.
registerRootComponent(App);

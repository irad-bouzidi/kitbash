# Every variable this application reads. Copy to .env and fill in.
# A missing one fails startup with a message naming it, rather than at the
# first request that happens to need it.

# Origin of the API. Empty means same-origin, which is what the dev server proxy and the container's nginx both arrange — set it only when the API lives elsewhere.
VITE_API_BASE_URL=

package {{ packageName }}.{{ entityName | camel }}.internal

internal class Duplicate{{ entityName }}NameException(
    name: String,
) : RuntimeException("A {{ entityName | camel }} named '$name' already exists")

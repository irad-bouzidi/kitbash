package {{ packageName }}.{{ entityName | camel }}.internal

import io.swagger.v3.oas.annotations.media.Schema
import java.time.Instant

@Schema(description = "A {{ entityName | camel }} as returned by the API")
internal data class {{ entityName }}Response(
    // Not `Long?`, although the entity's id is: a response is only ever built from a saved
    // {{ entityName | camel }}. Declaring it nullable put `"null"` in the OpenAPI document's type for `id`, and
    // every generated client then made callers handle an id that cannot be absent (§36).
    @field:Schema(example = "1", requiredMode = Schema.RequiredMode.REQUIRED)
    val id: Long,
    @field:Schema(example = "flux capacitor", requiredMode = Schema.RequiredMode.REQUIRED)
    val name: String,
    @field:Schema(example = "3", requiredMode = Schema.RequiredMode.REQUIRED)
    val quantity: Int,
    @field:Schema(requiredMode = Schema.RequiredMode.REQUIRED)
    val createdAt: Instant,
) {
    companion object {
        fun from({{ entityName | camel }}: {{ entityName }}): {{ entityName }}Response =
            {{ entityName }}Response(
                requireNotNull({{ entityName | camel }}.id) { "an unsaved {{ entityName | camel }} has nothing to return" },
                {{ entityName | camel }}.name,
                {{ entityName | camel }}.quantity,
                {{ entityName | camel }}.createdAt,
            )
    }
}

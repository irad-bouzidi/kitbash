package {{ packageName }}.{{ entityName | camel }}.internal

import io.swagger.v3.oas.annotations.media.Schema
import java.time.Instant

@Schema(description = "A {{ entityName | camel }} as returned by the API")
internal data class {{ entityName }}Response(
    @field:Schema(example = "1")
    val id: Long?,
    @field:Schema(example = "flux capacitor")
    val name: String,
    @field:Schema(example = "3")
    val quantity: Int,
    val createdAt: Instant,
) {
    companion object {
        fun from({{ entityName | camel }}: {{ entityName }}): {{ entityName }}Response = {{ entityName }}Response({{ entityName | camel }}.id, {{ entityName | camel }}.name, {{ entityName | camel }}.quantity, {{ entityName | camel }}.createdAt)
    }
}

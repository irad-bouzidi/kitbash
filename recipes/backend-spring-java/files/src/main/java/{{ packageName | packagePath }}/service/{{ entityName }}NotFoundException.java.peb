package {{ packageName }}.service;

public class {{ entityName }}NotFoundException extends RuntimeException {

    public {{ entityName }}NotFoundException(long id) {
        super("No {{ entityName | camel }} with id " + id);
    }
}
